import React from "react";
import { View, StyleSheet } from "react-native";

const AddToDo = () => {
    return <View className="bg-pink-500" style={styles.block} />;
};

const styles = StyleSheet.create({
    block: {
        height: 64,
    },
});

export default AddToDo;
